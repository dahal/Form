<div id="site-menu">
  <h4>Your Forms</h4>
  <hr>

  <% if current_user.forms? %>

    <div class="form-label">
      <%= current_user.forms.first.name %>
      <hr>
    </div>

  <% end %>
  <button>Create New Form</button>
  <p><%= current_user.email %> <%= link_to 'Sign out', destroy_user_session_path, method: :delete %></p>
</div>
